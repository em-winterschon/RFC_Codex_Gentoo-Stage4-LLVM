# Generated by gentoo-liveiso-ansible
# Include or translate these mappings into the active FreeRADIUS files backend or unlang policy.
{% for network_role in resolved_freeradius_bridge.group_mappings %}
DEFAULT LDAP-Group == {{ (network_role.freeipa_group | default(network_role.name)) | quote }}
{% for key, value in (network_role.radius_reply_items | default({})).items() %}
    {{ key }} := {{ value | quote }}
{% endfor %}

{% endfor %}
