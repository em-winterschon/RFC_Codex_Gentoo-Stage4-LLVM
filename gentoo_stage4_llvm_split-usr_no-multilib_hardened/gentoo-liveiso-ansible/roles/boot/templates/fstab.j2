# /etc/fstab: generated by gentoo-liveiso-ansible
{{ esp_partitions[0] }}  /efi   vfat  defaults,noatime  0  2
{% if swap_partitions | length > 0 -%}
{{ swap_partitions[0] }} none   swap  sw               0  0
{% endif -%}
