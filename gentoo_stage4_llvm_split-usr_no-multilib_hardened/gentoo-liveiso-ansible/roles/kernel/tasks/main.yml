---
- name: Install kernel config fragments
  ansible.builtin.include_role:
    name: kernel_config

- name: Install kernel and target package stack
  ansible.builtin.include_role:
    name: system_packages
