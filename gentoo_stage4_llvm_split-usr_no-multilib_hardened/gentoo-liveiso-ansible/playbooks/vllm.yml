---
- name: Deploy vLLM inference service
  hosts: vllm_servers
  gather_facts: true
  become: true

  vars:
    inference_service_backend: vllm
    inference_service_name: vllm
    inference_service_accelerator: nvidia

  roles:
    - role: inference_service
