---
- name: Deploy RFC1918 CA trust and service TLS material
  hosts: "{{ rfc1918_tls_target_hosts | default('all') }}"
  gather_facts: true
  become: true
  roles:
    - role: rfc1918_ca_trust
    - role: rfc1918_service_tls
