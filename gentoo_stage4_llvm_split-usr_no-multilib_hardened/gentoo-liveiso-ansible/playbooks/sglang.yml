---
- name: Deploy SGLang inference service
  hosts: sglang_servers
  gather_facts: true
  become: true

  vars:
    inference_service_backend: sglang
    inference_service_name: sglang

  roles:
    - role: inference_service
